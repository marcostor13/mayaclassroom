/**
 * Utilidades de texto. Se reexportan desde `@maya/shared` para que la API y el
 * cliente Angular apliquen exactamente el mismo saneado y los mismos resúmenes.
 */
export { stripHtml, excerpt, sanitizeHtml, randomCode, slugify } from '@maya/shared';
